# Supabase Client Requirements

# Core dependencies
supabase==2.3.4
python-dotenv==1.0.1

# Additional useful packages (optional)
pydantic==2.9.2  # For data validation
requests==2.31.0  # HTTP client (if needed separately)
